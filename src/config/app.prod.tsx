import { Config } from '.';

const config: Config = {
    authorizationCallbackUri: 'https://gh-gist.bndy.net/auth/callback',
    authorizationUri: 'https://cloud.bndy.net/service-oauth/authorize',
};

module.exports = config;
